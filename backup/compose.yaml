services:
  db:
    image: postgis/postgis:latest
    container_name: bitcraftmap-postgis
    restart: unless-stopped
    user: postgres
    environment:
      POSTGRES_INITDB_ARGS: "--auth=scram-sha-256"
      POSTGRES_DB: ${POSTGRES_DB_NAME?}
      POSTGRES_USER: ${POSTGRES_DB_ADMIN_NAME?}
      POSTGRES_PASSWORD: ${POSTGRES_DB_ADMIN_PASSWORD?}

# see later : POSTGRES_INITDB_WALDIR / POSTGRES_HOST_AUTH_METHOD / PGDATA 
# see https://www.docker.com/blog/how-to-use-the-postgres-docker-official-image/

    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - bitcraftmap-network
    volumes:
      - bitcraftmap-postgis-data:/var/lib/postgresql/data
      - ./initdb:/docker-entrypoint-initdb.d:ro

  pgadmin:
    depends_on:
      - db
    image: dpage/pgadmin4:latest
    container_name: bitcraftmap-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL?}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD?}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "8080:80"
    networks:
      - bitcraftmap-network
    volumes:
      - bitcraftmap-pgadmin-data:/var/lib/pgadmin
      - ./pgadmin/servers.json:/pgadmin4/servers.json:ro

networks:
  bitcraftmap-network:
    driver: bridge

volumes:
  bitcraftmap-postgis-data:
  bitcraftmap-pgadmin-data:

